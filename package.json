{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "test": "lerna run test",
    "postinstall": "lerna bootstrap",
    "publish": "lerna publish from-package --yes",
    "version": "lerna version"
  }
}
